<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.lumiroom.users.dao.UserMapper">

    <insert id="insertUser" parameterType="com.ssafy.lumiroom.users.dto.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password_hash, nickname, role, created_at)
        VALUES (#{email}, #{passwordHash}, #{nickname}, #{role}, NOW())
    </insert>

    <select id="findByEmail" parameterType="string" resultType="com.ssafy.lumiroom.users.dto.User">
        SELECT id, email, password_hash, nickname, role, created_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="uidByEmail" parameterType="string" resultType="Long">
        SELECT id
        FROM users
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" parameterType="string" resultType="int">
        SELECT EXISTS (
        SELECT 1 FROM users WHERE email = #{email}
        )
    </select>

</mapper>